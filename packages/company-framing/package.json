{
  "name": "@workflow/company-framing",
  "version": "0.0.0",
  "private": true,
  "description": "Company-level Pass 2A framing source intake helpers before formal case creation.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workflow/contracts": "workspace:*",
    "@workflow/persistence": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.4.5" }
}
