# @workflow/domain-support

**Owns** (handoff §5.11): domain support material — informational/non-company
sources, comparative-context shaping, terminology hints. Authority strictly
`informational_domain_support` (never `company_truth`).

**Does not own:** company-truth sources (`sources-context`), evaluation logic
(`synthesis-evaluation`), package contents (`packages-output`).

**Pass 1 status:** skeleton only. Domain support shaping lands in Pass 5.
