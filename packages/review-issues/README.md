# @workflow/review-issues

**Owns** (handoff §5.9): review issue lifecycle (gap-closure issues,
management-inquiry issues), reviewer/operator decisions, routing of release
decisions back into `core-state`.

**Does not own:** state transitions themselves (`core-state`), package contents
(`packages-output`), prompt rendering (`prompts`).

**Pass 1 status:** skeleton only. Issue model and lifecycle land in Pass 7.
