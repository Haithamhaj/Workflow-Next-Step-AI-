# @workflow/packages-output

**Owns** (handoff §5.8): Initial Package generation, Final Package generation,
as-is vs target-state comparison generation, output shaping logic.

**Does not own:** review/release decisions (`review-issues` + `core-state`),
package eligibility logic (held by core/state per spec §7.4).

**Pass 1 status:** skeleton only. Initial Package generation lands in Pass 6.
